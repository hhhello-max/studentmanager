spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/studentmanager?serverTimezone=UTC
    username: root
    password: 7777777
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB


mybatis:
  type-aliases-package: com.pjpy.studentManager.domain
  mapper-locations: classpath:mapper/*.xml

